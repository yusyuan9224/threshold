/// Module marker, kept for the linkage smoke test.
///
/// The adapter itself lives in `BLEClock.swift`, `BLEScanning.swift`,
/// `CentralManaging.swift`, `CoreBluetoothScanner.swift`, `FakeScanner.swift`
/// and `DeviceRegistry.swift` (docs/specs/bluetooth.md).
public enum ThresholdBluetoothModule { public static let name = "ThresholdBluetooth" }
